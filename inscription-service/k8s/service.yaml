apiVersion: v1
kind: Service
metadata:
  name: inscription-service
  namespace: devops
spec:
  selector:
    app: inscription-service
  ports:
    - port: 8097
      targetPort: 8097
      nodePort: 30098
  type: NodePort