apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret-promo
  namespace: devops
type: Opaque
data:
  mysql-root-password: cm9vdA==
  mysql-database: cGlkZXZfcHJvbW8=
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-promo
  namespace: devops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-promo
  namespace: devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-promo
  template:
    metadata:
      labels:
        app: mysql-promo
    spec:
      containers:
        - name: mysql
          image: mysql:8
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret-promo
                  key: mysql-root-password
            - name: MYSQL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: mysql-secret-promo
                  key: mysql-database
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: mysql-pvc-promo
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service-promo
  namespace: devops
spec:
  selector:
    app: mysql-promo
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  clusterIP: None
